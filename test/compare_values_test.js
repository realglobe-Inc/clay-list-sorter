/**
 * Test case for compareValues.
 * Runs with mocha.
 */
'use strict'

const compareValues = require('../lib/comparing/compare_values.js')
const assert = require('assert')
const co = require('co')

describe('compare-values', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Compare values', () => co(function * () {

  }))
})

/* global describe, before, after, it */
